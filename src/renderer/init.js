// import store from './store'

export default function init () {
  // 设置授权
  // store.dispatch('setAuthToken', window.localStorage.getItem('AUTH_TOKEN'))

  // 获取当前登录用户
  // store.dispatch('getCurrentUser')
}
